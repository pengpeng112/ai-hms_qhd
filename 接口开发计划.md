太好了，这里给你一份**“当前项目接口对接台账初版（可直接执行版）”**。  
我按你现在代码实际情况做了分层：**页面 → 按钮/动作 → 前端调用 → 后端接口 → 数据库表 → 当前状态/处理建议**。

---

# 接口对接台账初版（v0.1）

> 状态说明：  
> - ✅ 已接通（代码里有真实 REST 调用）  
> - ⚠️ 半接通（有接口但页面仍混用 mock / 回退 mock）  
> - ❌ 未接通（当前主要为 mock/本地状态）

---

## 1) 登录与认证

| 页面 | 按钮/动作 | 前端调用 | 后端接口 | 数据库表 | 状态 | 处理建议 |
|---|---|---|---|---|---|---|
| Login | 登录 | `performLogin` → `restApi.login` | `POST /api/v1/auth/login` | `users`（当前登录逻辑可能未读表） | ✅ | 先保持可用；后续决定是否切到数据库用户鉴权 |
| 全局 | 获取当前用户 | `restApi.getCurrentUser` | `GET /api/v1/me` | - | ✅ | 用于校验 token 生命周期 |

---

## 2) 角色选择与菜单

| 页面 | 按钮/动作 | 前端调用 | 后端接口 | 数据库表 | 状态 | 处理建议 |
|---|---|---|---|---|---|---|
| RoleSelect | 加载角色用户 | `getRoleUsersByGroup`（本地 mock） | 无 | - | ❌ | 增加后端接口：`GET /api/v1/roles/users`（建议） |
| MainLayout | 右侧任务栏列表 | `MOCK_TASKS` | 无 | - | ❌ | 后续新增任务中心接口，再替换 |

---

## 3) 患者列表页（优先级最高）

| 页面 | 按钮/动作 | 前端调用 | 后端接口 | 数据库表 | 状态 | 处理建议 |
|---|---|---|---|---|---|---|
| PatientList | 列表加载 | `restApi.getPatientList` | `GET /api/v1/patients` | `patients` | ✅ | 主链路可用 |
| PatientList | 新增建档 | `restApi.createPatient` | `POST /api/v1/patients` | `patients` + 可能 `patient_basic_infos` | ⚠️ | 校验 create 是否同步创建 basic-info |
| PatientList | 删除患者 | `restApi.deletePatient` | `DELETE /api/v1/patients/:id` | `patients`（及关联） | ✅ | 建议补“级联删除/软删”策略确认 |
| PatientList | 搜索/筛选 | 前端本地筛选 + 后端分页 | `GET /api/v1/patients`(query) | `patients` | ⚠️ | 后续将筛选全量改后端查询 |

---

## 4) 患者详情页（核心）

| 页面 | 按钮/动作 | 前端调用 | 后端接口 | 数据库表 | 状态 | 处理建议 |
|---|---|---|---|---|---|---|
| PatientDetail | 首屏加载 | `restApi.getPatientCore` | `GET /api/v1/patients/:id/core` | 聚合：`patients`/`treatment_plans`/`orders`/`lab_reports`等 | ✅ | 关键接口已在 |
| PatientDetail | 基本信息读取 | `restApi.getPatientBasicInfo` | `GET /api/v1/patients/:id/basic-info` | `patient_basic_infos` | ✅ | 可联调 |
| PatientDetail | 基本信息保存 | `restApi.updatePatientBasicInfo` | `PUT /api/v1/patients/:id/basic-info` | `patient_basic_infos` | ✅ | 必做字段校验 |
| PatientDetail | 临床病史读取 | `restApi.getMedicalHistory` | `GET /api/v1/patients/:id/medical-history` | `medical_histories` | ✅ | 可联调 |
| PatientDetail | 临床病史保存 | `restApi.saveMedicalHistory` | `PUT /api/v1/patients/:id/medical-history` | `medical_histories` | ✅ | 可联调 |
| PatientDetail | 转归记录增删改查 | `get/create/update/deleteOutcomeRecord` | `/api/v1/patients/:id/outcome-records` | `outcome_records` | ✅ | 可联调 |
| PatientDetail | 血管通路CRUD | `get/create/update/deleteVascularAccess` | `/api/v1/patients/:id/vascular-accesses` | `vascular_accesses` | ✅ | 可联调 |
| PatientDetail | 通路干预记录CRUD | `get/create/deleteVascularAccessIntervention` | `/api/v1/patients/:id/vascular-access-interventions` | `vascular_access_interventions` | ✅ | 可联调 |
| PatientDetail | 失败时回退 Mock | fallback `MOCK_PATIENTS` | - | - | ⚠️ | **建议立刻关闭 fallback**，避免假成功 |

---

## 5) 患者检验/检查/指标同步

| 页面 | 按钮/动作 | 前端调用 | 后端接口 | 数据库表 | 状态 | 处理建议 |
|---|---|---|---|---|---|---|
| 患者详情-检验 | 查询检验报告 | `restApi.getLabReports` | `GET /api/v1/patients/:id/lab-reports` | `lab_reports`,`lab_report_items` | ✅ | 可联调 |
| 患者详情-检验 | 同步检验报告 | `restApi.syncLabReports` | `POST /api/v1/patients/:id/lab-reports/sync` | 同上 | ✅ | 依赖 HDIS 配置 |
| 患者详情-检查 | 查询检查报告 | `restApi.getExamReports` | `GET /api/v1/patients/:id/exam-reports` | `exam_reports` | ✅ | 可联调 |
| 患者详情-检查 | 同步检查报告 | `restApi.syncExamReports` | `POST /api/v1/patients/:id/exam-reports/sync` | `exam_reports` | ✅ | 依赖 HDIS 配置 |
| 患者详情-指标 | 查询关键指标 | `restApi.getKeyIndicators` | `GET /api/v1/patients/:id/key-indicators` | `patient_key_indicators` | ✅ | 可联调 |
| 患者详情-指标 | 同步关键指标 | `restApi.syncKeyIndicators` | `POST /api/v1/patients/:id/key-indicators/sync` | `patient_key_indicators` | ✅ | 依赖 HDIS 配置 |

---

## 6) 治疗方案 / 医嘱 / 处方

| 页面 | 按钮/动作 | 前端调用 | 后端接口 | 数据库表 | 状态 | 处理建议 |
|---|---|---|---|---|---|---|
| 患者详情-治疗方案 | 方案 CRUD | `patientApi.*TreatmentPlan*` | `/api/v1/patients/:id/treatment-plan(s)` | `treatment_plans` | ✅ | 可联调 |
| 患者详情-方案调整记录 | 列表+新增 | `patientApi.get/createAdjustmentRecord` | `/api/v1/patients/:id/adjustment-records` | `adjustment_records` | ✅ | 可联调 |
| 患者详情-医嘱 | 多操作（建/改/停/分组等） | 页面是否全量接 `order` 需逐按钮核对 | `/api/v1/patients/:id/orders...` | `orders` | ⚠️ | 做按钮级核对（重点） |
| 患者详情-处方 | 列表/执行/取消等 | 页面是否全量接 `prescriptions` 需核对 | `/api/v1/patients/:id/prescriptions...` | `prescriptions` | ⚠️ | 做按钮级核对（重点） |

---

## 7) 字典配置（DictConfig）

| 页面 | 按钮/动作 | 前端调用 | 后端接口 | 数据库表 | 状态 |
|---|---|---|---|---|---|
| DictConfig | 字典类型列表/新增/编辑/删除 | `dictApi.listTypes/createType/updateType/deleteType` | `/api/v1/dict/types...` | `dict_types` | ✅ |
| DictConfig | 字典项列表/树/新增/编辑/删除/启停 | `dictApi.getItems/getItemsTree/createItem/updateItem/deleteItem/toggleItemEnabled` | `/api/v1/dict/items...` | `dict_items` | ✅ |
| DictConfig | 初始化字典 | `dictApi.initData` | `/api/v1/dict/items/init` | `dict_types`,`dict_items` | ✅ |

---

## 8) 诊疗配置中心（TreatmentConfig）

| 页面 | 按钮/动作 | 前端调用 | 后端接口 | 数据库表 | 状态 |
|---|---|---|---|---|---|
| TreatmentConfig-方案模板 | 列表/增删改/启停/设默认 | `treatmentConfigApi` | `/api/v1/treatment-templates...` | `plan_templates` | ✅ |
| TreatmentConfig-材料目录 | 列表/增删改/启停 | `treatmentConfigApi` | `/api/v1/materials/catalog...` | `material_catalogs` | ✅ |
| TreatmentConfig-药品目录 | 列表/增删改/启停 | `treatmentConfigApi` | `/api/v1/drugs/catalog...` | `drug_catalogs` | ✅ |
| TreatmentConfig-医嘱模板 | 列表/增删改/启停/条目 | `treatmentConfigApi` | `/api/v1/order-templates...` | `order_templates`,`order_template_items` | ✅ |

---

## 9) 设置页（Settings）

| 页面 | 按钮/动作 | 前端调用 | 后端接口 | 数据库表 | 状态 |
|---|---|---|---|---|---|
| Settings-Integration | 读取 HDIS 配置 | `restApi.getHdisIntegrationSettings` | `GET /api/v1/settings/integrations/hdis` | `integration_hdis_settings` | ✅ |
| Settings-Integration | 保存配置 | `restApi.updateHdisIntegrationSettings` | `PUT /api/v1/settings/integrations/hdis` | `integration_hdis_settings` | ✅ |
| Settings-Integration | 刷新 token | `restApi.refreshHdisToken` | `POST /api/v1/settings/integrations/hdis/refresh-token` | `integration_hdis_settings` | ✅ |
| Settings-Logs | 读取日志 | `restApi.getSystemLogs` | `GET /api/v1/settings/logs` | 日志文件（非业务表） | ✅ |

---

## 10) 当前明显“未接后端/假数据”页面

| 页面 | 现状 | 主要风险 |
|---|---|---|
| Dashboard | 混用 API + `MOCK_PATIENTS` + fallback | 看起来正常但不是数据库真实数据 |
| Schedule | 大量 `MOCK_STAFF / MOCK_SCHEDULE / MOCK_PATIENTS` | 排班按钮不落库 |
| Monitoring | `MOCK_MONITOR_DEVICES` | 监控数据非实时/非后端 |
| Inventory | 全量 Mock | 出入库不会写库 |
| MasterData | 全量 Mock | 主数据管理不写库 |
| DialysisProcessing | 大量 Mock/本地表单状态 | 透析执行过程不落库 |

---

# 你下一步按这个顺序做（建议）

1. **先把患者主链路打透**  
   `PatientList + PatientDetail + basic-info + medical-history + vascular + lab/exam sync`

2. **第二阶段打透“医嘱/处方/治疗方案”按钮级对接**

3. **最后替换 mock 页**  
   `Schedule → Monitoring → Inventory/MasterData/DialysisProcessing`

---

如果你同意，我下一步可以直接给你做 **“台账 v0.2（按钮级）”**：  
我会按每个页面把**具体按钮名**（比如“新增临时医嘱”“保存病史”“同步检验”）逐条列出来，并标上“该按钮现在调用了哪个函数、该函数是不是落到后端接口、该接口是否落哪张表”。这样你就能拿着表一项项验收。