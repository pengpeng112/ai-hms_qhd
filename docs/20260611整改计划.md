# AI-HMS 20260611评估整改计划（复核修订版）

> 修订日期：2026-06-11  
> 依据：`docs/20260611ai评估/` 原始报告、交付补丁、当前源码逐项核对  
> 当前结论：其他 AI 的整改方向总体正确，但原计划把多个“补丁已交付待合入”的问题误写成“当前已修”。本文以当前工作区源码为准。

---

## 一、复核结论

### 1. 原整改计划需要修订的误判

| 原计划判断 | 复核结论 | 证据 |
|---|---|---|
| 患者管理“透析中判定已修” | 未修 | `patient_core_service.go:139-143` 仍查 `models.Treatment` + snake_case 字段 `patient_id/status/treatment_date` |
| 患者管理“透析龄已修” | 未修 | `patient_core_service.go:133-134` 仍用 `patient.CreatedAt` 起算 |
| 患者管理“导航后端已修” | 未修 | `patient_core_service.go:449-455` 仍全表加载，且 TenantId 查询失败后回退全量跨租户 |
| v2 错误格式“已修” | 未完全修 | `api.go:54-55` 中 `tenantMiddleware` 在 `responseWrapper` 前；`responseWrapper` 仅包装 2xx，不归一 4xx/5xx |
| 医嘱/处方/监控/资源补丁“已修” | 未合入 | 当前源码仍缺 `TenantId`、`StopReason`、监控时间判据、UFGoal 修复等 |
| EndTime “UpdateStatus 未修” | 正确 | `treatment_service.go:1438-1449` 完成状态仍不写 `EndTime` |

### 2. 当前真正已存在的基础能力

| 能力 | 当前状态 |
|---|---|
| smart_schedule 路由、排班状态码、上机/下机接口 | 已存在 |
| `TreatmentService.Update` 状态完成时补 `EndTime` | 已存在，见 `treatment_service.go:1392-1399` |
| 安全头、登录限流、SeedAdmin 默认关闭、权限码迁移、临床安全基础包 | 已存在 |
| 前端 `smartScheduleApi` 测试、编码治理、`hdis_access_token` 修复 | 已存在 |

---

## 二、修订后的整改任务清单

### P0：可直接改的安全和数据一致性问题

| 编号 | 任务 | 当前问题 | 修改文件 | 是否需人工确认 |
|---|---|---|---|---|
| P0-1 | v2 错误响应归一 | v2 4xx/5xx 仍返回 `{code,error}`，且 401/403 因中间件顺序绕过 wrapper | `internal/smart_schedule/api/api.go`，新增 `responsewrapper_test.go` | 否 |
| P0-2 | 患者核心信息修复 | “透析中”判定查错表；透析龄用建档时间；导航全表加载且跨租户兜底 | `internal/services/patient_core_service.go` | 否 |
| P0-3 | `UpdateStatus` 下机补 `EndTime` | 完成状态只改 `Status`，不写 `EndTime` | `internal/services/treatment_service.go` | 否 |
| P0-4 | 医嘱停嘱原因落库 + Stop 补 TenantId | `stopReason` 被丢弃；Stop UPDATE 无 `TenantId` | `internal/services/order_service.go` | 否，但需接受写入 `Note` 的方案 |
| P0-5 | 处方写操作补 TenantId | `LegacyUpdate/Execute/Cancel` UPDATE 仅按 `Id` | `internal/services/prescription_service.go` | 否 |
| P0-6 | 实时监控修复 | ①按 `Status='30'` 过滤→本系统上机病人不可见 ②`UFGoal` 原设 `DryWeight`（干体重）→错 ③Ward 联表缺 TenantId ④`UFVolume` 应来自 `Device_DMLog` 而非 `Treatment_DuringParam` | `internal/services/monitoring_service.go` | 否 |
| P0-7 | 病区/床位删除补 TenantId | `WardService.Delete`、`BedService.Delete`、床位设备关系软删缺 TenantId | `internal/services/ward_service.go`、`bed_service.go` | 否 |

### P1：需要人工确认或已由数据库核实

> **数据库核实日期**：2026-06-11，目标库 `10.20.1.153:5432/dialysis`（老血透库）。  
> 查询 SQL 和结果详见本节各项。

| 编号 | 任务 | 核实结论 | 当前代码与修正方向 |
|---|---|---|---|
| P1-1 | 治疗状态写老库映射 | **已核实，可以改**。老库 `Treatment_Treatment.Status` 字典真实值：`0=待签到 / 10=签到 / 20=透前评估 / 30=透中监测 / 40=透后评估 / 50=取消治疗 / 60=已结束 / 70=首次核对 / 80=设备消毒 / 90=耗材登记 / 100=健康宣教...180=费用确认`。码值线性且非简单三态，老 Python 系统用这套完整流程码。 | **可执行修改**：`legacyStatusFromApp` 改为 `InProgress→"30"`（透中监测）、`Completed→"60"`（已结束，✓不变）、`Cancelled→"50"`（取消治疗）、`Pending→"0"`（待签到）。同时 `appStatusFromLegacy` fallback 把 `"0"/"10"` 映射调整为仅按码值读字典（让字典的 `Name` 驱动语义），不再硬猜。 |
| P1-2 | 治疗记录删除改软删 | **已核实**。`Treatment_Treatment` **没有 `IsDisabled` 列**（已查 information_schema.columns，无此列）。该表有 `CaseStatus` 列（`varchar(64)`），字典为 `10=治疗 / 20=封存`。 | Delete 应改为写 `CaseStatus='20'`（封存），且建议同时写 `EndTime=COALESCE("EndTime",now)`。**不复用 IsDisabled，封存方案已确认可行**。 |
| P1-3 | Dashboard 治疗状态映射 | **部分核实**。Dashboard 后端当前按 `Status='60'` 算今日完成数（`dashboard_service.go:148`），但 `treatmentStatusText` 前端同时维护 app 层 `'1'/'2'/'3'` 和老库 `'10'/'20'/'30'` 两套映射。**人工仍需确认**：今日治疗列表接口（`getTodayTreatments` 或等效 path）返回的 Status 究竟是哪一层。 | 确认后的修复：Dashboard 统一只保留一套映射，与后端实际返回值对齐。 | 
| P1-4 | 处方 `Note` 存 JSON | **已核实，风险确认存在**。① 老系统在 `Plan_PatientPrescription.Note` 中存的是纯文本（如 "单超10min/h"、"盐水间断冲洗透析器"）；② 新系统已向该列写入了 3 条 JSON 记录 `{"prescriptionDate":"2026-05-31","doctorName":"test_admin"}`。**人工仍需确认**：老 Python 系统打开处方时是否展示 `Note` 列、是否可编辑。如果旧系统展示/编辑该字段，JSON 会直接裸显示给医生（不可读），且老系统编辑会覆盖 JSON → 新系统结构化数据全部丢失。 | 现有代码 `prescription_service.go:150-156` 构造 JSON 并写入 Note。确认前不改 Note 写入方案（属于跨系统设计冲突，需业务/老系统维护人决定是把新增字段还是扩表）。 |
| P1-5 | 医嘱未来停用日期语义 | **已人工确认**：未来停用日期按“到期停”处理，不应当场 `IsDisabled=true`。当前 `order_service.go:851-865` 无论 `stopDate` 为何均立即停用，需修。 | 如果 `stopDate` 晚于当前日期，只写 `EndTime=stopAt` 和停嘱原因，不立即置 `IsDisabled=true`；到期后由列表状态判定/定时任务处理停用。 |
| P1-6 | 删非空病区策略 | **已人工确认**：非空病区禁止删除，提示先迁移/停用床位。 | `WardService.Delete` 删除前检查启用床位数量，`BedCount>0` 时返回业务错误；仍补 TenantId。 |

### 附加：处方状态映射（数据库 + 源码已确认）

`Plan_PatientPrescription.Status` 字典：`1=草稿 / 2=确认 / 3=作废`。当前数据 `Status=1 (8条) / 2 (24157条)`。

源码已确认：`prescription_service.go:93-105` 中 `mapNewPrescriptionStatus` 将 `PrescriptionStatusExecuting` 映射为 `1`，即写入老库后语义变成“草稿”。这是确定的问题。

**已人工确认修正策略**：先按老库状态来即可。老库没有“执行中”码值，因此新系统不应把 `Executing` 写成 `1=草稿`；应按老库 `1=草稿 / 2=确认 / 3=作废` 对齐。

### P2：低风险前端/体验修复

| 编号 | 任务 | 修改文件 | 说明 |
|---|---|---|---|
| P2-1 | DIALYSIS_MODE fallback 补齐 | `CreatePatientModal.tsx`，必要时 `TreatmentConfig` 类型联合 | fallback 现有 `HD/HFD/HDF/HF`，缺 `CRRT`，部分分析报告还提到不同位置缺 `HP/HD+HP`，需统一以字典/排班模式为准 |
| P2-2 | PatientDetail 切换患者去掉 `pageSize:1000` | `PatientDetail.tsx` | 优先改用后端 `patient-core` navigation；若前端已有 navigation 字段，直接消费 |
| P2-3 | 后端 stub 功能前端隐藏 | 医嘱修订/复制/从模板、诊疗配置设默认、库存新增/删除/调整 | 这些后端明确返回“暂不支持”，避免一线误操作 |
| P2-4 | UI一致性分批做 | 共享组件/token/页面机械替换 | 不与 P0 临床数据修复混在一个提交，降低回归范围 |

---

## 三、详细代码修改计划

### P0-1：v2 错误响应归一

修改：`ai-hms-backend/internal/smart_schedule/api/api.go`

1. 调整中间件顺序：

```go
rg.Use(responseWrapper())
rg.Use(tenantMiddleware())
```

2. 扩展 `responseWrapper`：

```go
// 2xx: {success:true,data,timestamp}
// 4xx/5xx: {success:false,error:{code,message},timestamp}
```

3. 新增辅助函数：

```go
func writeWrapped(w gin.ResponseWriter, status int, body []byte)
func normalizeError(m map[string]interface{}, isObj bool, status int) (string, string)
```

4. 新增测试：`internal/smart_schedule/api/responsewrapper_test.go`

测试覆盖：成功包装、历史错误归一、无 code 回退 HTTP status、已统一格式透传、中间件 abort 归一。

验证：

```powershell
go test ./internal/smart_schedule/api -count=1
go test ./internal/smart_schedule/... -count=1
```

### P0-2：患者核心信息修复

修改：`ai-hms-backend/internal/services/patient_core_service.go`

1. `buildHeader` 透析龄：

```go
ageStart := patient.CreatedAt
if patient.AdmissionDate != nil && !patient.AdmissionDate.IsZero() {
    ageStart = *patient.AdmissionDate
}
```

说明：模型里 `AdmissionDate` 已确认通过 `gorm:"column:FirstDialysisDate"` 映射老库首次透析日期，见 `internal/models/patient.go:58`。

2. `buildHeader` 透析中判定：

```go
s.db.Table(`"Treatment_Treatment"`).
  Where(`"TenantId" = ? AND "PatientId" = ? AND "StartTime" IS NOT NULL AND "EndTime" IS NULL AND DATE(COALESCE("StartTime","SignInTime","ReceptionTime","CreateTime")) = DATE(?)`, LegacyTenantID, patient.ID, time.Now()).
  Count(&onMachineToday)
```

3. `buildNavigation` 改索引化查询：

```go
base := func() *gorm.DB { return s.db.Model(&models.Patient{}).Where(`"TenantId" = ?`, LegacyTenantID) }
Count(total), Count(before), 分别取 `< patientID` 和 `> patientID` 的一条记录。
```

4. 删除 TenantId 查询失败后全量兜底逻辑。

验证：

```powershell
go test ./internal/services -run PatientCore -count=1
go test ./internal/services -count=1
```

### P0-3：`UpdateStatus` 下机补 `EndTime`

修改：`ai-hms-backend/internal/services/treatment_service.go`

在 `UpdateStatus` 中追加：

```go
if status == models.TreatmentStatusCompleted {
    updates["EndTime"] = gorm.Expr(`COALESCE("EndTime", ?)`, time.Now())
}
```

同时，因为 P1-1 已核实老库状态码字典，**`UpdateStatus` 中状态码写入也需要同步修正**：

当前 `UpdateStatus` 第 1443 行写 `legacyStatusFromApp(status)` — 其映射将在 P1-1 修正后自动生效。

补充建议：如果完成状态也应同步 smart_schedule 状态 60，应在确认调用路径后追加：

```go
if status == models.TreatmentStatusCompleted {
    s.syncScheduleStatus(id, 60)
}
```

不确定点：当前主治疗服务 `SubmitPostAssessment` 已同步 60，`UpdateStatus` 是否也应同步 60 需确认是否存在重复路径。

验证：

```powershell
go test ./internal/services -run Treatment -count=1
```

### P1-1：修复 legacyStatusFromApp 状态码映射（数据库已核实，可执行）

修改：`ai-hms-backend/internal/services/treatment_service.go`，函数 `legacyStatusFromApp`。

修正前：
```go
func legacyStatusFromApp(status int) string {
    switch status {
    case models.TreatmentStatusInProgress:
        return "10"   // ❌ 签到
    case models.TreatmentStatusCompleted:
        return "60"   // ✓ 已结束
    case models.TreatmentStatusCancelled:
        return "90"   // ❌ 耗材登记
    default:
        return "0"    // ✓ 待签到
    }
}
```

修正后：
```go
func legacyStatusFromApp(status int) string {
    switch status {
    case models.TreatmentStatusInProgress:
        return "30"   // ✓ 透中监测（治疗中）
    case models.TreatmentStatusCompleted:
        return "60"   // ✓ 已结束
    case models.TreatmentStatusCancelled:
        return "50"   // ✓ 取消治疗
    default:
        return "0"    // ✓ 待签到
    }
}
```

依据：2026-06-11 查询 `10.20.1.153/dialysis` 库 `CodeDictionary_CodeDictionarys` 中 `Type='Treatment_TreatmentStatus'` 字典确认。

同时需要修正 `appStatusFromLegacy`（第 1246 行）中 `"0"/"10"` 的 fallback 分支：这两个码现在不应再被 hardcode 为 `Pending/InProgress`，而应优先读 `statusDict`（传自 `Treatment_TreatmentStatus` 字典的 `Name` 值），只对无法匹配的码才用时间判据。

验证：
```powershell
go test ./internal/services -run Treatment -count=1
```

### P1-2：治疗记录删除改软删（数据库已核实，可执行）

修改：`ai-hms-backend/internal/services/treatment_service.go`，函数 `Delete`。

当前：
```go
result := s.db.Table(`"Treatment_Treatment"`).
    Where(`"Id" = ? AND "TenantId" = ?`, id, LegacyTenantID).
    Delete(nil)
```

改为：
```go
updates := map[string]any{
    "CaseStatus":     "20",  // 封存
    "EndTime":        gorm.Expr(`COALESCE("EndTime", ?)`, time.Now()),
    "LastModifyTime": time.Now(),
}
result := s.db.Table(`"Treatment_Treatment"`).
    Where(`"Id" = ? AND "TenantId" = ? AND COALESCE("CaseStatus", '10') <> '20'`, id, LegacyTenantID).
    Updates(updates)
```

依据：`Treatment_Treatment` 无 `IsDisabled` 列；`CaseStatus` 列字典 `10=治疗 / 20=封存`。

验证：
```powershell
go test ./internal/services -run Treatment -count=1
```

### P0-4：医嘱停嘱原因落库 + Stop 补 TenantId

修改：`ai-hms-backend/internal/services/order_service.go`

1. 增加 `unicode/utf8` import。

2. 在 `updates` 中追加 Note 表达式：

```go
if reason := strings.TrimSpace(stopReason); reason != "" {
    const maxReasonLen = 200
    if utf8.RuneCountInString(reason) > maxReasonLen {
        reason = string([]rune(reason)[:maxReasonLen])
    }
    suffix := "\n[停嘱原因 " + time.Now().Format("2006-01-02 15:04") + "] " + reason
    updates["Note"] = gorm.Expr(`LEFT(COALESCE("Note", '') || ?, 1024)`, suffix)
}
```

3. Stop UPDATE 补 TenantId：

```go
Where(`"Id" IN ? AND "TenantId" = ?`, intIDs, LegacyTenantID)
```

4. `getOrder` 是否也补 TenantId：建议补，因为当前是 `Id + PatientId`，虽然归属已较强，但与“写前查也限定租户”的惯例一致：

```go
Where(`"Id" = ? AND "PatientId" = ? AND "TenantId" = ?`, oid, legacyPID, LegacyTenantID)
```

验证：

```powershell
go test ./internal/services -run Order -count=1
```

### P0-5：处方写操作补 TenantId

修改：`ai-hms-backend/internal/services/prescription_service.go`

三处：

```go
Where(`"Id" = ? AND "TenantId" = ?`, item.ID, LegacyTenantID)
```

范围：`LegacyUpdate`、`LegacyExecute`、`LegacyCancel`。

验证：

```powershell
go test ./internal/services -run Prescription -count=1
```

### P0-6：实时监控修复

修改：`ai-hms-backend/internal/services/monitoring_service.go`

**数据源修正（关键）**：当前监控服务的 `UFVolume` 来自 `Treatment_DuringParam.UFQuantity`（护理手动录入表，可能为空），`UFGoal` 来自 `Plan_PatientPlan.DryWeight`（干体重 ≠ 目标超滤量）。正确数据源是 `Device_DMLog`（透析机实时上传表）：

| 监控字段 | 错误来源（当前） | 正确来源 | 字段 |
|---------|---------------|---------|------|
| UFGoal | Plan_PatientPlan.DryWeight | Device_DMLog | `UFSetVolume`（设定超滤总量） |
| UFVolume | Treatment_DuringParam.UFQuantity | Device_DMLog | `UFVolume`（实际累计超滤量） |
| TreatmentTime | 无 | Device_DMLog | `TreatmentTime`（已透析分钟数） |

1. 增加当天窗口：

```go
now := time.Now()
todayStart := time.Date(now.Year(), now.Month(), now.Day(), 0, 0, 0, 0, now.Location())
```

2. Ward 联表补 TenantId：

```go
LEFT JOIN "Schedule_Ward" AS w ON w."Id" = t."WardId" AND w."TenantId" = t."TenantId"
```

3. 主查询从状态码改为时间判据：

```go
Where(`t."TenantId" = ? AND t."StartTime" IS NOT NULL AND t."EndTime" IS NULL AND t."StartTime" >= ? AND t."StartTime" < ?`, tenantID, todayStart, todayStart.AddDate(0, 0, 1))
```

4. 新增 `Device_DMLog` 子查询，取每个治疗最近一条设备数据，提取 `UFSetVolume`、`UFVolume`、`TreatmentTime`：

```go
// 取每个治疗的最新设备数据
type deviceRow struct {
    TreatmentID   int64
    UFSetVolume   float64
    UFVolume      float64
    TreatmentTime float64
}
var devices []deviceRow
db.Table(`"Device_DMLog"`).
    Select(`DISTINCT ON ("TreatmentId") "TreatmentId", "UFSetVolume", "UFVolume", "TreatmentTime"`).
    Where(`"TenantId" = ? AND "TreatmentId" IN ?`, tenantID, treatmentIDs).
    Order(`"TreatmentId", "LogTime" DESC`).
    Scan(&devices)
```

5. 组装结果时从设备数据取值：

```go
if dev, ok := deviceMap[r.ID]; ok {
    d.UFGoal = dev.UFSetVolume   // 目标超滤量
    d.UFVolume = dev.UFVolume     // 实际超滤量
    // TreatmentTime 可追加用于进度计算
}
// Fallback: 无设备数据时 UFGoal / UFVolume 均为 0（前端触发"暂无超滤数据"降级）
```

验证：

```powershell
go test ./internal/services -run Monitoring -count=1
```

### P0-7：资源管理删除补 TenantId

修改：`ward_service.go`、`bed_service.go`

1. `WardService.Delete`：

```go
Where(`"Id" = ? AND "TenantId" = ?`, id, LegacyTenantID)
```

2. `BedService.Delete`：

```go
Where(`"Id" = ? AND "TenantId" = ?`, id, LegacyTenantID)
```

3. 床位设备关系软删：

```go
Where(`"BedId" = ? AND "TenantId" = ?`, id, LegacyTenantID)
```

注意：`syncBedEquipments` 当前 `Delete(nil)` 是物理删关系，且创建关系时未显式写 `TenantId`，这不属于原补丁范围，但建议单独复核老库表是否要求 `TenantId NOT NULL`。

验证：

```powershell
go test ./internal/services -run 'Ward|Bed' -count=1
```

---

## 四、人工复核清单（带数据库核实结论）

### 1. 治疗状态码真实字典 ✅ 已核实

已于 `2026-06-11` 查询 `10.20.1.153/dialysis`：

```sql
SELECT "Code", "Name" FROM "CodeDictionary_CodeDictionarys"
WHERE "Type" = 'Treatment_TreatmentStatus'
ORDER BY "Sort", "Code";
```

**结果（19 个码值）**：

| 码值 | 名称 | 数据量（Treatment_Treatment.Status） |
|------|------|------------------------------------|
| 0 | 待签到 | 113 条 |
| 10 | 签到 | — |
| 20 | 透前评估 | 2 条 |
| 30 | 透中监测 | 22 条 |
| 40 | 透后评估 | — |
| 50 | 取消治疗 | 52 条 |
| 60 | 已结束 | 19,227 条 |
| 70 | 首次核对 | 2 条 |
| 80 | 设备消毒 | — |
| 90 | 耗材登记 | 43 条 |
| 100 | 健康宣教 | 4,585 条 |
| 110 | 当日处方 | 5 条 |
| 120 | 透析医嘱 | 1 条 |
| 130 | 编辑病区床位 | 5 条 |
| 140 | 获取设备开始治疗时间 | — |
| 150 | 二次核对 | 34 条 |
| 160 | 护士小结 | — |
| 170 | 医生小结 | — |
| 180 | 费用确认 | — |

**结论**：老库 `Treatment_Treatment.Status` 不是简单的 10/30/50/60 三态，而是一个完整的 0→180 诊疗流程码。当前 `legacyStatusFromApp` 应修正为：
- `InProgress → "30"`（透中监测），不是当前 `"10"`（签到）
- `Completed → "60"`（已结束），当前已正确 ✓
- `Cancelled → "50"`（取消治疗），不是当前 `"90"`（耗材登记）
- `Pending → "0"`（待签到）

### 2. 治疗记录删除策略 ✅ 已核实

`Treatment_Treatment` **没有 `IsDisabled` 列**（information_schema.columns 确认）。

该表有 `CaseStatus` 列（`varchar(64)`），字典 `10=治疗 / 20=封存`。

**可执行的软删方案**：`Delete` 改为写 `CaseStatus='20'`（封存），同时写 `EndTime=COALESCE("EndTime", now)`。

### 3. Dashboard 状态来源 ⚠ 仍需人工确认

数据库无法回答接口返回层问题。需确认：
- `getTodayTreatments` / Dashboard 治疗列表 API 返回的 `Status` 是 app 层（`0/1/2/3`）、老库原始码（`10/30/60`）、还是中文名。确认后再统一前端映射。

### 4. 处方 Note 存 JSON ⚠ 部分核实，仍需确认老系统行为

数据库确认：
- 老系统在 `Plan_PatientPrescription.Note` 中存的是**纯文本**（"单超10min/h"、"盐水间断冲洗透析器"）
- 新系统已向同表同列写入了 **3 条 JSON**：`{"prescriptionDate":"2026-05-31","doctorName":"test_admin"}`
- Plan_PatientPlan.Note 也是纯文本（"每月2次..."、"盐水间断冲洗透析器"）

**仍需确认**：老 Python 系统是否在处方展示/编辑界面中显示 `Note` 列。如果显示，JSON 会裸曝光给医生。

### 5. 字典 fallback 码值风格 ✅ 已核实

```
ExpenseType Code = 中文名（"市职工普通"、"现金"等），非数字码
IDType Code = 中文名（"居民身份证"、"社会保障卡"等）
DialysisMethod Code = 字母码（HD/HDF/HFD/HF/HP/CRRT/PE/HD+HF/IUF+HD 等 16 种）
```

前端 fallback 与老库一致即可；ExpenseType 用中文名 Code 是正常的。

### 6. 处方状态映射（数据库 + 源码已确认）⚠ 需确认修正策略

`Plan_PatientPrescription.Status` 字典：`1=草稿 / 2=确认 / 3=作废`。当前数据 `Status=1 (8条) / 2 (24,157条)`。

源码已确认：`mapNewPrescriptionStatus` 当前确实把 `Executing → Status=1`，即“执行中”写成了老库“草稿”。

仍需你确认：由于老库只有 `草稿/确认/作废`，新系统“执行中”应折叠为 `2=确认`，还是前端/后端不再暴露“执行中”流转。

---

## 五、仍需人工确认项（清晰版）

> 这里仅保留“数据库 + 表结构 + 当前源码都无法直接决定”的业务选择。能从数据库和表结构确定的项，已经从此清单移出。

| 项 | 已能确定的事实 | 还需要你确认的业务选择 | 我建议的默认处理 |
|---|---|---|---|
| Dashboard 状态映射 | 前端 `getTodayTreatments()` 不是调用 Go 后端，而是通过 `src/services/api.ts` 访问老 Python/HDIS GraphQL：`Treatment(parameters:...)`，并请求字段 `Status`。因此数据库只能证明老库 `Treatment_Treatment.Status` 是流程码，不能证明 GraphQL 返回是否原样透传、转成 app 状态、还是中文。 | 请确认接口返回的 `Treatment.Status` 实际值：例如 `30/60`、`1/2`、`透析中/已完成`。 | 在未确认前，不改 Dashboard 状态映射。等拿到一条接口样例后统一映射。 |
| 处方 `Plan_PatientPrescription.Note` 存 JSON | 数据库已证明老数据多为纯文本备注；新系统已经写入 3 条 JSON。表结构只说明 `Note=text`，无法判断老 Python 页面是否展示/编辑该列。 | 老 Python 系统处方页面是否显示/允许编辑 `Plan_PatientPrescription.Note`？如果会显示，JSON 会裸露给医生。 | 若老系统显示/编辑 Note：停止继续写 JSON，改为纯文本备注或另找扩展字段/表；若老系统不展示：暂可保留但需文档化。 |  **已确认：note字段在前端展示为备注，是医生录入的**
| 医嘱未来停用日期 | 表结构：`Order_PatientOrder.EndTime` 是“医嘱停用时间，可空”，`IsDisabled` 是禁用状态。当前代码传未来日期也立即 `IsDisabled=true`。 | **已确认：到期停**。未来日期只写 `EndTime` 和停嘱原因，不应立即 `IsDisabled=true`。 | 可直接修改。 |
| 删除非空病区 | 表结构：`Schedule_Ward` 和 `Schedule_Bed` 都有 `IsDisabled`。当前列表已经计算 `BedCount`，说明系统知道病区下是否有启用床位。 | **已确认：禁止删除非空病区**。提示先迁移/停用床位。 | 可直接修改。 |
| 处方“执行中”状态策略 | 数据库已证明 `Plan_PatientPrescription.Status` 只有 `1=草稿/2=确认/3=作废`。源码已证明 `Executing→1` 是错的。 | **已确认：先按老库状态来**。不要把“执行中”写成老库“草稿”。 | 可直接修改映射，并隐藏/取消不兼容的“执行中”流转入口。 |

---

## 六、建议执行顺序

1. 先合入 P0-1 到 P0-7（包括 P1-1 和 P1-2，因两者已通过数据库核实现可直接执行）。
2. 每合一个模块跑对应单测；全部完成后跑后端整体验证。
3. 仍需人工确认后才可以改：P1-3 (Dashboard 状态层)、P1-4 (Note JSON)。P1-5、P1-6、处方状态映射策略已人工确认，可直接执行。
4. 最后做 P2 前端体验和 UI 一致性，避免临床数据修复与大面积 UI diff 混在一起。

后端整体验证：

```powershell
go test ./internal/services ./internal/api/v1 ./internal/middleware ./internal/smart_schedule/... ./internal/clinicalsafety/ -count=1
go vet ./cmd/server ./internal/...
go build -o "$env:TEMP\check.exe" ./cmd/server
```

前端验证：

```powershell
npm run lint
npm run build
npx vitest run
```

---

## 七、当前建议

可直接执行：P0-1 ~ P0-7、P1-1 (状态码映射)、P1-2 (治疗软删)、P1-5 (医嘱未来日期到期停)、P1-6 (非空病区禁止删除)、处方状态映射按老库状态修正。

仍不要擅自改：P1-3 (Dashboard 状态映射需确认接口返回层)、P1-4 (Note JSON 需确认老 Python 系统行为)。
