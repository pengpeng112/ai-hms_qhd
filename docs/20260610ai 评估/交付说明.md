# AI-HMS 稳定性修复 — 交付说明

> 交付日期：2026-06-10
> 基线提交：`4334917`（feat: 补充测试文件 + userOf修复 + 老库对接方案与交接文档）
> 本交付 = 在基线之上的 **6 个独立提交**，17 个文件，+1139/-50。
> 不依赖远程仓库，全部通过本 ZIP 离线交付。

---

## 一、本次改了什么（6 个提交）

| 顺序 | 提交 | 类型 | 内容 |
|---|---|---|---|
| 1 | `219b1ab` | fix | **P0** 移除运行时 DDL：排班唯一索引改为启动只校验、不再对老生产库 `CREATE INDEX` |
| 2 | `4ac7ff6` | fix | **P1** 默认管理员播种改默认关闭、口令不再硬编码、日志不打印明文 |
| 3 | `9e90e05` | docs | 系统评估报告 + 改进计划 + 老库写操作保障核查 |
| 4 | `f29cb79` | fix | 修复 GBK 乱码（Monitoring.tsx）+ 新增 UTF-8 编码闸门与 .editorconfig |
| 5 | `101a6e1` | feat | admin 门禁支持权限码 + 集中管理员角色真值源（带角色兜底，零锁死） |
| 6 | `13771eb` | feat | 安全响应头中间件（CSP Report-Only，零破坏）+ JWT 有效期文档化 |

> 这 6 个提交**不包含**当前分支 `fix/legacy-ui-restore` 上你们正在进行的老库迁移 WIP（约 300 个未提交改动）。两者互不干扰。

---

## 二、如何合入（三选一）

### 方式 A：git bundle（推荐，保留完整提交历史）

在你们的项目仓库根目录执行：

```bash
# 1) 确认本地有基线提交 4334917（rar 解出的仓库本就有）
git cat-file -t 4334917        # 应输出 commit

# 2) 从 bundle 取回 6 个提交到一个临时分支
git fetch ./ai-hms-stabilization.bundle HEAD:stabilization-fixes

# 3) 评审后合入你们的目标分支（按需 merge 或 cherry-pick）
git log --oneline 4334917..stabilization-fixes     # 看这 6 个提交
git checkout <你们的目标分支>
git merge stabilization-fixes                       # 或逐个 cherry-pick
```

> bundle 是增量包，要求仓库里已存在基线 `4334917`（你们一定有）。

### 方式 B：format-patch 补丁（便于逐个评审 / 不方便用 bundle 时）

`patches/` 下有 6 个 `.patch`，按编号顺序应用：

```bash
git am patches/*.patch
# 若 git am 因换行符/上下文报错，可改用：
git apply --reject --whitespace=fix patches/0001-*.patch   # 逐个，处理 .rej
```

### 方式 C：仅评审，不直接应用

`patches/*.patch` 是标准 unified diff，可直接阅读，或导入代码评审工具人工誊写。

---

## 三、⚠️ 上线必做（部署 / DBA 动作）

1. **【DBA】排班唯一索引**（对应提交 1）：
   应用已不再自动建索引。请由 DBA 执行
   `ai-hms-backend/scripts/schedule_unique_indexes.sql`：
   - **先跑脚本里的"重复数据探测"**（必须返回 0 行），再建索引；
   - 用 `CREATE UNIQUE INDEX CONCURRENTLY` 避免锁表；
   - 索引缺失时应用仍能启动，并在 `GET /api/v2/schedule/health` 的 warnings 中标红。

2. **【运维】默认管理员播种**（对应提交 2）：
   现默认**不再**自动创建管理员。如确需播种，设置：
   `SEED_ADMIN_ENABLED=true` + `SEED_ADMIN_USERNAME=...` + `SEED_ADMIN_PASSWORD=...`
   （口令不得使用已知/共享值；生产不建议开启）。详见 `.env.example`。

3. **【运维】安全响应头 / JWT**（对应提交 6）：
   - 安全响应头已默认全局启用；CSP 为 **Report-Only**（只观察不拦截），
     联调确认无误后可在 `internal/middleware/security_headers.go` 升级为强制 CSP。
   - HSTS 未在应用层下发，建议由前置 Nginx 在 HTTPS 处统一配置。
   - `JWT_EXPIRATION_HOURS` 当前 24，建议按一个工作班次调短（如 8~12），见 `.env.example`。

---

## 四、restClient.ts 编码修复（独立补丁，需单独应用）

`ai-hms-frontend/src/services/restClient.ts` 有 78 处同类 GBK 乱码，**未并入前述 6 个提交**——
因为该文件同时有你们进行中的迁移改动，乱码修复与之交织于同一文件，为避免误带你们的 WIP 而单独交付。

本 ZIP 提供了 `patches/restClient-encoding.patch`（**仅含 78 处乱码行的 1:1 替换，不含任何代码/WIP 改动**），
已对该文件的 `HEAD` 基线版本验证 `git apply --check` 通过、应用后内容与目标一致、乱码检测归零。

**应用方式（在仓库根目录，用 `git apply` 而非 `git am`）：**

```bash
git apply patches/restClient-encoding.patch
python scripts/check_encoding.py ai-hms-frontend/src   # 复核：应输出"编码检查通过"
git diff ai-hms-frontend/src/services/restClient.ts    # 人工复核后随迁移提交一并合入
```

> 若你们对该文件已有大量本地改动导致补丁上下文不匹配，可改用脚本兜底（确定性 gbk→utf8 逆转，正常中文自动跳过）：
> ```bash
> python scripts/fix_encoding.py ai-hms-frontend/src/services/restClient.ts
> ```
> 检测脚本 `scripts/check_encoding.py` 已随提交 4 进入仓库根 `scripts/`。

---

## 五、验证与作者信息

- 全部 6 个提交均通过：`gofmt` / `go build ./...` / `go vet` / 相关单测（middleware、sched）/ 编码闸门。
- 提交作者记为 `angela <angela19781120@gmail.com>`（本地 git 身份）。
  如需改为团队规范的作者信息，合入前可用 `git rebase -i` + `--amend` 调整。
- 配套分析文档（评估报告 / 改进计划 / 写操作核查）随提交 3 一并带入，位于仓库根目录。

---

## 六、ZIP 内容清单

```
ai-hms-stabilization.bundle           # 方式 A：git bundle（6 个提交）
patches/0001..0006-*.patch            # 方式 B：format-patch 补丁（6 个提交）
patches/restClient-encoding.patch     # 第四节：restClient.ts 乱码修复（git apply）
scripts/fix_encoding.py               # restClient.ts 等乱码的修复脚本（补丁不适用时的兜底）
交付说明.md                            # 本文件
```
</content>
