# 待确认问题清单

核查日期：2026-05-31

以下问题在本次核查中无法确定答案，需要你确认后告知后续处理方向。

## 1. 医嘱"从模板组调取"功能是否需要实现？

**当前状态**：已隐藏弹窗中的"从模板组调取"标签页，只保留"直接新增录入"模式。

**问题**：old_system 截图/HAR 中可能存在医嘱模板组接口。如果业务上需要从模板组调取医嘱，需要：
- 后端补充 `GET /order-templates`、`POST /orders/from-template` 等接口
- 前端恢复模板 tab 并完成交互

**你的决定**：□ 需要实现 / □ 暂不需要，保持隐藏 / □ 其他：___

---

## 2. 角色管理：删除 vs 停用

**当前状态**：`Authorization_Roles` 表只有 5 列（Id, OrganId, GroupId, Name, LastModifyTime），没有状态/禁用字段。

**问题**：old_system 截图状态列显示 `active`，操作列显示"停用"而非"删除"。但老库表没有状态字段可做软停用。

**可选方案**：
- A) 保持物理删除（当前行为），操作列仍显示"删除"
- B) 在 `Authorization_Roles` 表新增 `IsDisabled` 列实现软停用
- C) 将角色关联的用户记录清除后标记为"已停用"（前端常量展示）

**你的决定**：□ A / □ B / □ C / □ 其他：___

---

## 3. 当日处方动态调整记录是否需要实现？

**当前状态**：`TodayPrescription.tsx` 中"当日处方动态调整记录"表格固定显示"暂无数据"。

**问题**：old_system 截图要求展示处方修改历史。实现需要：
- 确认老库是否有处方变更日志表（如有，提供表名）
- 后端提供变更日志查询接口
- 前端接入真实数据

**你的决定**：□ 需要实现 / □ 暂不需要，保持"暂无数据" / □ 其他：___

---

## 4. 同一患者同日是否允许多条排班/治疗记录？

**当前状态**：透析执行左侧患者列表以 `patient.id` 为行 key，同一患者只显示一行，只加载当天一条治疗记录。

**问题**：old_system 截图中可见同一患者可能有多条排班记录（如不同床位/班次）。如果业务上允许同日多条：
- 患者列表需要以排班/治疗记录为粒度而非患者粒度
- `getPatientTreatmentByDate` 需要支持班次/床位参数
- 需要确认 `Schedule_Treatment` 或类似排班表

**你的决定**：□ 允许同日多条，需改造 / □ 同日只有一条，保持现状 / □ 其他：___

---

## 5. 病区负责医护字段格式确认

**当前状态**：`Schedule_Ward.ResponsibleUsers` 是 `character varying`，前端改为多选用户下拉后提交 comma-separated ID 字符串。

**问题**：后端保存时用逗号拼接 ID 列表，回显时需要反向解析。需要确认：
- 老系统该字段是否确实存储逗号分隔的 ID？
- 列表展示的 `responsibleUserNames` 是否需要后端联查用户名？

**你的决定**：□ comma-separated ID / □ 完整姓名列表 / □ 需要查老数据确认 / □ 其他：___

---

## 6. 床位设备绑定关系确认

**当前状态**：`Schedule_BedEquipmentRel` 表存在（BedId, EquipmentId, IsDefault, Sort），后端已支持，但前端编辑弹窗暂无设备列表交互。

**问题**：old_system 截图中床位编辑可配置设备。前端需补充设备选择器，但这需要：
- 接入 `Auxiliary_EquipmentInfomation` 的设备列表
- 支持多选、标记默认设备
- 当前前端暂未实现，是否需要本轮完成？

**你的决定**：□ 本轮完成 / □ 后续迭代 / □ 其他：___
